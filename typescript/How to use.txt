TypeScript
TypeScript is the super set of JavaScript (ES6) but it not supported by all browser,
TypeScript doesn’t run on the browser we need to compile it to JavaScript and run in the browser	
some of TypeScript features are Generics, Interfaces and Strongly Typed language

How to use

Run "npm install" to install the required dependencies

Run "tsc" to compile the TypeScript code

Run "npm start" to run the development server (lite-server)
